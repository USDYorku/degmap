/**
 * Function to simplyfiy checking if object is defined
 * 
 * @param {type} obj object to be checked
 * @returns {Boolean} true if object is passed is undefined
 */
function isUndefined(obj) {
    return typeof obj === "undefined";
}